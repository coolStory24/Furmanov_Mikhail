name: Simple CI job

on:
  push:
    branches: ['main']
  pull_request:
    branches: ['main']

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Print date to console
        run: |
          echo $(date +"%A, %b %d, %Y %H:%M:%S")
