name: Print date CI job

on:
  push:
    branches: ['main', 'dev']
  pull_request:
    branches: ['main', 'dev']

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Print date to console
        run: |
          echo $(date +"%A, %b %d, %Y %H:%M:%S")
